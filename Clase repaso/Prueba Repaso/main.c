#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <conio.h>
#include "BibliotecaPruebaRepaso.h"

int main()
{
    menuDeOpciones();
/* dos criterios, nombre y nota
int compararAlumnos(alumno1, alumno2)
{
    int retorno = 0;
    if(alumno1.nota > alumno2.nota)
    {
        retorno = 1;
    }
    else if (alumno1.nota < alumno2.nota)
    {
        retorno = -1;
    }
    else if (strcmp(alumno1.nombre, alumno2.nombre)>0)
    {
        retorno = 1;
    }
    else if (strcmp(alumno1.nombre, alumno2.nombre)<0)
    {
        retorno= -1;
    }
    return retorno;

*/
    return 0;
}
